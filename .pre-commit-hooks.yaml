- id: agent-security-scanner
  name: Agent Security Scanner
  description: Scan AI Agent, MCP, AI coding tool, shell, secret, and GitHub Actions risks.
  entry: agent-scan
  language: python
  pass_filenames: false
  always_run: true
  args: [".", "--fail-on", "high"]
